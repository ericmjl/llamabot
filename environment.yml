name: llamabot
channels:
  - conda-forge
dependencies:
  # Core stack
  - python=3.11
  - build
  - bump2version
  - ipykernel
  - ipywidgets
  - jupyter
  - jupyterlab
  - mamba
  - pip
  - twine
  - loguru
  # Utilities
  - pyprojroot
  - python-dotenv
  # CLI development
  - typer>=0.4.0
  # Software testing
  - hypothesis
  - pytest
  - pytest-cov
  - coverage
  # Code Style Tools
  - black
  - darglint
  - flake8
  - interrogate
  - pre-commit
  - pylint
  # Documentation
  - mkdocs
  - mkdocs-material
  - mkdocstrings
  - panel
  - jupyter_bokeh
  - bokeh<=3.1.0
  - pyzotero
  - pyperclip
  - astunparse
  - pytest-mock
  # pip dependencies
  - pip:
      # Place packages that cannot be installed by conda in this section.
      - openai
      - langchain>=0.0.189
      - llama_index>=0.6.0
      - mknotebooks
      - wget
      - outlines
